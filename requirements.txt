# Core pipeline dependencies (CPU is enough - the models are tiny).
# For a CUDA build of PyTorch, install it from https://pytorch.org instead.

numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
torch>=2.0
optuna>=3.4
matplotlib>=3.7
seaborn>=0.13
